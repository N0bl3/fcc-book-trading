extends layout
block content
    h1 All Books for non auth user
    if message.length
        div.alert.alert-danger= message
    if books
        each book in books
            div.book
                h1= book.title
                h2= book.owner
    else
        p No books to show for now
